---
const links = [
  {href: "/zonas", label: "Zonas"},
  {href: "/mapa", label: "Mapa"},
  {href: "/contacto", label: "Contacto"},
];
---

<footer
  class="bg-gradient-to-r from-blue-700 to-green-600 backdrop-blur-sm py-8">
  <div class="w-full mx-auto p-4 md:py-8 container max-w-6xl">
    <div
      class="flex flex-col md:flex-row items-center justify-between gap-6 sm:flex sm:items-center sm:justify-between">
      <!-- Sección izquierda -->
      <div class="text-center md:text-left">
        <h4
          class="self-center text-xl font-medium whitespace-nowrap text-white w-full max-w-xl sm:text-2xl md:text-4xl tracking-tighter text-pretty pointer-events-none">
          Cancún SafePlace
        </h4>
        <p
          class="text-white block text-sm sm:text-center font-light tracking-tighter text-balance pointer-events-none">
          Pasos cortos, vidas largas. Información confiable para explorar Cancún
          con seguridad.
        </p>
      </div>
      <div class="flex gap-11 mt-6 md:mt-0 text-white">
        {
          links.map((links) => (
            <a
              href={links.href}
              class=" text-md font-light tracking-tighter text-balance hover:text-yellow-300">
              {links.label}
            </a>
          ))
        }
      </div>
    </div>
    <hr class="my-6 border-white sm:mx-auto" />
    <div
      class="block text-sm text-white sm:text-center font-light tracking-tighter text-balance pointer-events-none">
      © {new Date().getFullYear()} Protecto TeecBack.
    </div>
  </div>
</footer>
